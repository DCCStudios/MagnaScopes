[Diagnostics]
; Stage 5a inspects the loaded OG executable only. It does not install the
; auxiliary renderer hooks, change scope rendering, or apply zoom/camera
; overrides. The resulting log supplies exact in-memory prologue bytes and
; data-page validation for the next guarded stage.
VerificationStage=5
CameraOverride=0
TAACapture=0
VisualProbe=0
GeometryProbe=0
GeometryMagnification=0

[AutoSTS]
Enabled=1
DefaultMaskDiameter=700.0
DefaultMagnification=2.0
ZoomSpread=1.5
